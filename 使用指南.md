# CocaCode Dock 使用与维护指南

> 面向**非开发人员**的说明。你不需要懂代码，照着做就行。
> 软件名：**CocaCode Dock** ｜ 作者：**CocaCode** ｜ 主页：https://github.com/G-zorro

---

## 一、你实际要用的文件（成品）

直接使用的只有这一个文件夹：

```
GameDock/
└── dist/
    └── win-unpacked/          ← 整个文件夹一起复制使用
        ├── CocaCodeDock.exe   ← 双击它启动程序
        ├── 使用说明.txt        ← 程序内的使用说明
        └── resources/
            └── app/           ← 程序内部源码副本（不用管）
```

**用法**：把 `win-unpacked` 整个文件夹复制到任意位置（比如 D 盘或桌面），
双击里面的 `CocaCodeDock.exe` 即可。删掉这个文件夹就等于卸载，不留垃圾。

> ⚠️ 不要只复制 `CocaCodeDock.exe` 一个文件，必须连 `resources` 文件夹一起，否则打不开。

---

## 二、开发源文件在哪（给以后升级用）

源代码在 `src/` 目录，已用 git 备份，随时可改、可升级：

```
GameDock/
├── src/
│   ├── main/                  ← 程序"大脑"（启动、扫描、托盘、右键功能）
│   │   ├── main.js
│   │   ├── preload.js
│   │   ├── data-schema.js
│   │   └── store.js
│   └── renderer/              ← 程序"长相"（界面、按钮、弹窗）
│       ├── index.html
│       ├── styles.css
│       └── app.js
├── package.json               ← 项目信息（名称/作者/版本）
├── README.md                  ← GitHub 发布说明
├── CHANGELOG.md               ← 版本更新记录
├── LICENSE                    ← 开源协议（MIT）
└── 使用指南.md                ← 本文件
```

**备份状态**：`src/` 已通过 git 提交保存（`git log` 可见历史）。
发布到 GitHub 后，源码会再有一份云端备份，更安全。

---

## 三、如何修改升级（二次开发）

> 只有当你想改功能、加功能时才需要看这节。单纯使用不需要。

### 准备工作（只需做一次）
1. 安装 **Node.js**（官网 https://nodejs.org ，下 LTS 版，一路下一步）。
2. 进入项目文件夹，在地址栏输入 `cmd` 回车，执行：
   ```
   npm install
   ```

### 改完代码后，更新成品
本项目是**手动打包**（免签名、最简单）：
1. 改完 `src/` 里的文件后，把它们复制到成品目录：
   ```
   src/main/*.js        →  dist/win-unpacked/resources/app/src/main/
   src/renderer/*       →  dist/win-unpacked/resources/app/src/renderer/
   ```
2. 双击 `dist/win-unpacked/CocaCodeDock.exe` 看效果。

> 不需要 `npm run build`、不需要签名，改完复制即可，对电脑小白最友好。

---

## 四、发布到 GitHub（开源）

> 目的：把项目公开到 https://github.com/G-zorro ，既是发布也是云端备份。

### 方式 A：让助手帮你推（推荐）
1. 在 WorkBuddy 左侧「连接器」里找到 **GitHub**，点「连接」并登录你的账号。
2. 回来告诉我"已连接"，助手会帮你：
   - 新建公开仓库 `CocaCode-Dock`
   - 把整个项目（含 `src/` 源码）推上去
   - 完成开源发布

### 方式 B：你自己操作（备用）
1. 打开 https://github.com/G-zorro ，点右上角 **New**（新建仓库）。
   - Repository name 填：`CocaCode-Dock`
   - 选 **Public**（公开/开源）
   - 不要勾选 "Add a README"（我们已有）
   - 点 Create repository
2. 把仓库地址（形如 `https://github.com/G-zorro/CocaCode-Dock.git`）发给助手，
   助手会设置并推送。

> 注意：`node_modules/`（依赖）和 `dist/`（成品）已配置忽略，**不会**上传，
> 仓库只含源码和说明文档，干净轻量。

---

## 五、常见问题

**Q：换电脑了怎么办？**
A：从 GitHub 下载（或复制 `GameDock` 文件夹），双击 `CocaCodeDock.exe` 即可，配置存在程序内。

**Q：程序打不开？**
A：确认是**整个 `win-unpacked` 文件夹**一起复制的，不是只复制了 exe。

**Q：想彻底退出、不留托盘？**
A：当前是"关闭窗口收起托盘、不退出"（你早期定稿）。若想改成"关闭即彻底退出"，
   告诉我，我改一处功能即可。

**Q：源码会丢吗？**
A：本地 `src/` + git 提交 +（发布后）GitHub 云端，三重保险，不会丢。
